.content-header
    .container-fluid
        .row.mb-2
            .col-sm-6
                h1
                    strong #{title}
            .col-sm-6
                ol.breadcrumb.float-sm-right
                    li.breadcrumb-item
                        p Server
                    li.breadcrumb-item.active #{guild.name}